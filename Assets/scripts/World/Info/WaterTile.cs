using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterTile : ITile
{
    public WaterTile(int X, int Y, GameObject gameObject, int index) : base(X, Y, gameObject, index)
    {
    }
}
